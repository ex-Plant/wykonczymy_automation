# Review-gate ledger — blob-store-isolation · 2026-08-19

Scope: 5 commits on `konradantonik/blob-store-isolation` vs `staging`.
Fan-out: `/10x-impl-review`, `/code-review`, structure+comment-noise (combined), then `/simplify`.
Dropped checks: `tailwind-v4-audit` (no UI in the diff), Step 0.5 verification pass
(no browser-verification skill installed in this project).

## Findings

- [x] 🟡 WARNING · fixed · code-review · `src/lib/env/schema.ts:37` · the guard was one-directional — PRODUCTION carrying the **preview** token passed silently, so real client invoices would be written into a store that gets wiped and re-restored as scratch. Now symmetric (your call, 2026-08-19). Deliberately still **not** an allow-list: an unrecognised store id passes in both directions, so rotating a store can never refuse a production boot on a stale constant. Renamed `prodBlobTokenRefusal` → `blobTokenRefusal`, since it no longer guards one store.
      test: TDD · unit — the preview-in-production refusal asserted at both call layers (`serverSchema` and the predicate), plus an explicit case pinning that an unrecognised store still boots.
- [x] 🔵 OBSERVATION · dropped · code-review · `src/lib/env/schema.ts:37` · `VERCEL_ENV` is an ordinary env var, so one `vercel env pull --environment=production` into `.env.local` poisons **both** of the guard's inputs at once (Next prefers `.env.local`). Hardening it — also demanding a genuine Vercel runtime signal such as `VERCEL_DEPLOYMENT_ID` — adds a second way to refuse a production boot for a scenario that needs someone to hand-swap a token in production. Dropped on your call, 2026-08-19; the `.env.local` trap stays documented in `AGENTS.md` and the runbook.

- [x] 🔴 CRITICAL · fixed · code-review + impl-review F1 · `src/payload.config.ts:42` · the guard did not sit on the path that deletes: the Blob plugin takes its token from raw `process.env`, and nothing in the `(payload)` graph imports `@/lib/env`, so `/admin/collections/media` reached the production store unvalidated under `next dev`. Now `payload.config.ts` runs the shared `blobTokenRefusal` at the line that hands over the token. Verified `pnpm generate:types` still passes — importing `schema.ts` from the CLI graph was untested and was impl-review's stated blind spot.
      test: TDD · unit — `blobTokenRefusal` covered directly across all four `VERCEL_ENV` values, because that call site parses no schema.
- [x] 🟡 WARNING · fixed · impl-review F5 · `AGENTS.md:156`, `scripts/blob-snapshot.mjs:1` · docs claimed `BLOB_READ_WRITE_TOKEN_PROD` existed "for the backup scripts only", but **no script reads it** — so a snapshot run off bare `.env` silently captured the PREVIEW store and reported success, the same failure shape as the nightly DB backups that dumped the wrong database for weeks. Docs corrected to the explicit-export form; the snapshot tool now prints its target store id on every run.
      test: no automated test — a doc claim plus a console line; the observable guard is the printed store id, checked by eye at run time.
- [x] 🟡 WARNING · fixed · impl-review F3 + code-review · `scripts/blob-restore.mjs:51` · `token.split('_')[3]` made the guard **fail open**: an unparseable token yielded `undefined`, matched nothing, and proceeded to a bulk upload. Replaced with an allow-list — anything that is not the preview store needs `--allow-prod`, so a rotated store, a second production store and a malformed token all fail closed. Verified empirically across four branches with fake tokens.
      test: no automated test — dependency-free recovery script with no spec harness; exercised by hand on all four branches (prod-shaped, unparseable, preview, bad `--concurrency`), recorded in `## Tests & suite`.
- [x] 🟡 WARNING · fixed · code-review · `src/__tests__/lib/env/schema.test.ts:8` · the spec built its production token from the very constant it was testing, so it asserted only that the guard agreed with itself — a typo'd or rotated id would leave the suite green with a dead guard. Token is now a hardcoded literal, plus an explicit assertion pinning `PROD_BLOB_STORE_ID`.
      test: TDD · unit — the finding IS the test change.
- [x] 🟡 WARNING · fixed · impl-review F2 + code-review · `scripts/blob-refresh-preview.sh:33` · `"$@"` forwarded `--allow-prod` straight into the write tool, so a command named `refresh-preview` could write to production. Now refused before the mirror runs. `--limit` likewise refused, because the wrapper passes its own first and `arg()` takes the first match — a caller's smaller limit would have been silently ignored.
      test: no automated test — verified by running the wrapper with each flag; both exit 1 before any network call.
- [x] 🟡 WARNING · fixed · code-review · `scripts/blob-refresh-preview.sh:51` · the FTP password was spliced into the lftp command string, where a comma truncates it (lftp's `user,pass` separator) and a quote escapes into lftp commands; it was also visible in `ps aux` for the length of the mirror. Now passed via `LFTP_PASSWORD` + `--env-password`.
      test: no automated test — credential path; asserting it would require a live FTPS server.
- [x] 🟡 WARNING · fixed · code-review · `scripts/blob-refresh-preview.sh:69` · unbounded `put()` burst in the very script whose header documents the 2026-08-19 account-wide suspension. Capped at `BLOB_REFRESH_MAX` (default 500) and announced, not silent; resumable via `--skip-existing`.
      test: no automated test — the cap is a constant passed to a tool whose own dry-run reports the count.
- [x] 🟡 WARNING · fixed · impl-review F8 · `scripts/blob-restore.mjs:60` · `--concurrency abc` → `Array.from({length: NaN})` → zero workers → "Uploaded 0/1771 · OK", exit 0. A green no-op is the worst failure mode a recovery tool has. Now validated, and the run exits non-zero if any file was never attempted.
      test: no automated test — verified by hand (`--concurrency abc` now exits 1).
- [x] 🟡 WARNING · fixed · impl-review F6 · `.github/workflows/blob-backup.yml:19`, runbook §3 · nothing stated that the GitHub Actions secret must stay the PRODUCTION token; the `--min-blobs` floor would not have caught a swap, since preview holds ~2347 files. Stated in both places.
- [x] 🟡 WARNING · fixed · impl-review F4 · `context/foundation/lessons.md` · the entry omitted the symptom phase 4 contracted for (a 404 on a recent invoice in local dev is expected) and `pnpm blob:refresh:preview`. Added, together with the lesson the 🔴 taught: an env-layer guard covers only the graph that parses it.
- [x] 🟡 WARNING · fixed · code-review + impl-review F1(b) · `context/foundation/manual-checks.md:1372` · the check asserted `pnpm dev` "odmawia startu", which is false — Next compiles lazily. Reworded to what actually happens, and split into three checks so the `/admin` path (the one that deletes) is verified separately from `(frontend)`.
- [x] 🔵 OBSERVATION · fixed · code-review · `scripts/blob-refresh-preview.sh:60` · `lftp -c` does not reliably propagate a failure exit code, so a failed auth was indistinguishable from a healthy "nothing to do" run. The mirror is now asserted non-empty.
- [x] 🔵 OBSERVATION · fixed · impl-review F10 · `AGENTS.md:162`, runbook · the pointer to runbook §3 resolved to a section that said nothing about the refresh flow. Runbook §5 gained a subsection (lftp prereq, cache dir, the cap); the `--dry-run` usage line now admits it still lists the target under `--skip-existing`.
- [x] 🔵 OBSERVATION · fixed · impl-review F7 + module-cohesion · `scripts/blob-restore.mjs:45` · the store id was duplicated under two different names (`PROD_STORE_ID` / `PROD_BLOB_STORE_ID`) with two different matchers, so a `grep` for the constant found only one of the two copies that must stay in sync. Renamed and cross-referenced; the duplication itself stays, justified by the scripts' dependency-free constraint.
- [x] 🔵 OBSERVATION · fixed · impl-review F9 · `plan.md` · the whole-tree gate had no Progress rows, so its status was unrecorded. Added, including the pre-existing lint failures the branch does not inherit.
- [x] 🔵 OBSERVATION · fixed · code-review · `scripts/blob-restore.mjs:30` · `arg()` took the next token unconditionally, so `--dir --allow-prod` both set the dir and satisfied the guard. A flag is now never read as a value.
- [x] fixed · simplify · `src/lib/env/schema.ts:29` · the two guard call sites each hand-wrote the condition, the message and the `VERCEL_ENV`-not-`NODE_ENV` rationale — the exact drift the comments warn about. Collapsed into one exported predicate (`blobTokenRefusal`).
- [x] fixed · simplify · `src/__tests__/lib/env/schema.test.ts:93` · spec follows the consolidation and now covers the predicate across all four `VERCEL_ENV` values.
- [x] fixed · comment-noise · `vitest.config.ts:17` · trimmed the alias comment to the load-bearing clause (why the alias is not a no-op); the rest restated the line above it.
- [x] dropped · simplify · `scripts/blob-restore.mjs` ↔ `blob-mirror.mjs` ↔ `blob-snapshot.mjs` · `arg` / `fmtMB` / `sleep` / the API base URL / cursor pagination are each written 2–3×. Real, but predates this slice and a shared `scripts/blob-store.mjs` is a review-worthy refactor of three recovery tools, not a cleanup.
- [x] dismissed · simplify · `scripts/blob-restore.mjs:51` ↔ `blob-snapshot.mjs:35` · the token regex written twice: no `.mjs` under `scripts/` has ever imported a sibling, and "copy one file anywhere and run it" is the property that gets used during an incident. Two one-line call sites is under the threshold.
- [x] dismissed · simplify · lftp FTPS preamble across five files · the copies are not identical (they diverge on `xfer:clobber` / `passive-mode`), the workflows already factor theirs into `LFTP_OPTS`, and this slice's copy is deliberately different — it alone routes the password through `--env-password`.
- [x] dismissed · simplify · `scripts/blob-restore.mjs:166` · the two consecutive failure exits look redundant but emit different diagnostics; the second is fail-closed insurance against exactly the green-no-op this slice fixed.
- [x] dismissed · code-review · `src/lib/env/schema.ts:41` · `serverSchema` is no longer a `ZodObject`, so `.shape` / `.extend` are gone. Nothing in the tree uses them.
- [x] dismissed · simplify · `scripts/blob-refresh-preview.sh:22` · `[ -f .env.local ] && source .env.local` under `set -euo pipefail` looks like the classic guarded-command trap; tested rather than assumed — bash does not exit, since a failing `&&` list only trips `set -e` on its final command.
- [x] dropped · simplify · `scripts/blob-mirror.mjs:25` · its `arg()` copy still has the flag-as-value bug this slice fixed elsewhere. No possible caller: the script is CI-only and the workflow passes fixed, well-formed arguments.
- [x] dropped · code-review · `src/__tests__/lib/env/schema.test.ts:13` · `baseEnv` hand-mirrors the required-var list, so a new required var breaks all four tests at once. Fails loudly rather than silently, and no full-env factory exists to reuse.
- [x] dropped · code-review · `vitest.config.ts:17` · regex aliases would remove the declaration-order dependency, but the ordering is documented in place and exact-match regexes change resolution semantics for no behavioural gain.
- [x] skipped · code-review · `scripts/blob-refresh-preview.sh:52` · `ssl:verify-certificate no` leaves the FTPS session unauthenticated against MITM. Matches the two existing workflows against the same host; changing it here alone risks breaking the mirror on that host's certificate, and the fix belongs to all five call sites at once.
- [x] dismissed · feature-first-structure / module-cohesion / structure-scatter · zero findings across all three: every file landed in its correct home, `src/__tests__/lib/env/` is the mandated mirror rather than a new home, and `PROD_BLOB_STORE_ID` sits with the only rule that consumes it.

## Simplify pass

Ran `/simplify` (with `primitive-reuse-scan`) — 2 applied, 1 proposed, 7 dismissed; each folded into `## Findings` above tagged `simplify`. The single proposal (a caller's `--limit` being silently ignored) was resolved in-thread by refusing the flag rather than deferring it.

## Tests & suite

- `pnpm exec vitest run src/__tests__/lib/env/schema.test.ts` — 9 passed (was 4; the guard predicate and the store-id pin are new).
- `pnpm typecheck` — clean.
- `pnpm generate:types` — clean, twice: the 🔴 fix introduces the first import of `@/lib/env/schema` into the Payload CLI graph, which `server-only` would have broken.
- `scripts/blob-restore.mjs` guard, by hand with fake tokens: prod-shaped → exit 1; unparseable → exit 1 (was exit 0, the fail-open); preview → exit 0; `--concurrency abc` → exit 1. No network call in any branch.
- `scripts/blob-refresh-preview.sh`: `bash -n` clean; `--allow-prod` → exit 1; `--limit` → exit 1. Both refuse before touching FTP.
- Whole-tree gate, after the symmetric-guard fix: `pnpm typecheck` clean · `pnpm test` 2544 passed / 134 skipped (221 files) · `pnpm build` clean. `pnpm lint` not re-run — its 2 errors predate this branch (recorded in `plan.md` G.3).
- `pnpm test:e2e` deliberately not run: this slice ships no UI, and the guards it adds are asserted at the unit layer and by hand at the CLI. No E2E obligation, so nothing owed to the E2E backlog.
- The build is itself evidence the new branch does not over-fire: `next build` sets `NODE_ENV=production` while `.env` holds the preview token, and the guard correctly stayed silent because it keys on `VERCEL_ENV`.
