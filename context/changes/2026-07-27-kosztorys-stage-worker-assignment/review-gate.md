# Review-gate ledger — kosztorys-stage-worker-assignment (EX-613) · 2026-07-28

Diff under review: `afda6fc3~1..HEAD` (Phases 1–3, commits `afda6fc3`, `3f18d858`, `d9b3a06a`).

Fan-out run 2026-07-29: `/10x-impl-review` · `/code-review` · `tailwind-v4-audit` ·
`feature-first-structure` · `module-cohesion-audit` · `structure-scatter-audit` (diff-scoped) ·
`comment-noise-audit` (flag-only). Tailwind and scatter came back clean; 0 critical findings.

## Findings

<!-- Format: [box] [severity] · disposition · `source` · `file:line` · what — reason -->

- [x] 🟡 WARNING · deferred+filed **EX-636** · `code-review` · `src/lib/queries/subcontractor-roster.ts:65` · plane-less etapy are a silent blind spot in the wypłata dialog — `subcontractorDueByPlane` skips `plane === null` before touching `byStage`/`byWorker`, so a plane-less etap with real work counts as 0 and `unassignedStageCount` never fires; the editor panel has `hasUnconfirmedPlane` to explain it, `SubcontractorRosterT` carries no equivalent. Owner reads a data-entry gap as an overpayment. Deferred: a third warning surface is a design decision beyond this slice's two agreed warnings.
      test: test-driven-debugging · unit — assert the roster's counts for an investment whose etapy have `plane === null` with quantities entered; recorded in EX-636 so the guard travels with the fix.
- [x] 🔵 OBSERVATION · deferred+filed **EX-637** (`e2e-backlog`) · `impl-review` · `plan.md:434` · E2E obligation still open — the „Podsumowanie podwykonawców" block and the wypłata roster are browser-untested. Filed with the `pickComboOption` export (`e2e/helpers.ts:80`) noted as its prerequisite.
      test: e2e — deferred with the fix into EX-637.

- [x] 🟡 WARNING · fixed · `code-review` · `src/components/forms/hooks/use-roster.ts:45` · `resetRoster()` bumps `requestRef`, so an in-flight request's `finally` no-ops and `isRosterLoading` is stranded at `true` forever — `PayoutRosterSummary` then renders „Wczytywanie rozliczenia…" permanently. Diverges from the `useSaldo` it mirrors, whose `resetSaldo` deliberately leaves `requestRef` alone.
      test: test-driven-debugging · unit — red repro asserts `isRosterLoading === false` after a reset supersedes an in-flight load.
- [x] 🟡 WARNING · fixed · `code-review` · `src/lib/kosztorys/subcontractor-summary.ts:80` · float residue misclassifies a fully-settled worker as `overpaid`, in red — `due` is `Σ qty × viewPrice` (fractional coefficients), `paid` a raw `SUM(amount)`, neither rounded; paying the displayed amount exactly yields `remaining = -1.1e-13`, which reads „nadpłata / pozostało -0,00" in destructive red. This is the paid-in-full case, i.e. the most common one, and it is newly introduced.
      test: test-driven-debugging · unit — red repro pays the rounded `due` and asserts `state === 'settled'`, not `overpaid`.
- [x] 🟡 WARNING · fixed · `impl-review`+`code-review` · `src/components/kosztorys/editor/grid/stage-header.tsx:60` · an etap held by a since-deactivated worker reads as unassigned — `currentWorkerName` is resolved inside the already-filtered `activeWorkers`, so no checkbox ticks (nor „Bez przypisania"), and the loud reassign confirm quotes „nieznana osoba". `SubcontractorWorkerTotals` uses the unfiltered list and still names them, so two surfaces disagree about who holds the etap.
      test: test-driven-debugging · unit — assert the resolved current-holder name for an inactive assignee.
- [x] 🟡 WARNING · fixed · `impl-review` · `src/lib/kosztorys/subcontractor-summary.ts:113` · the „Bez przypisanego pracownika" residual row gets a person-shaped qualifier — `assignedWorkerIds` contains `null`, so the null bucket reaches `settlementState` with `hasStages === true` and renders „przypisane etapy bez wykonanych prac"; assign every etap and the same row flips to „brak przypisanych etapów". Both qualifiers are sentences about a person, attached to a residual that is not one.
      test: test-driven-debugging · unit — assert the null row's `state` with some etapy unassigned and with all assigned.
- [x] 🟡 WARNING · fixed · `impl-review`+`code-review` · `src/components/forms/form-fields/entity-combobox-field.tsx:64` · `isActiveRef` was extracted with a comment claiming it is „named once because getting it backwards silently empties a picker", but the pre-existing inline `item.active !== false` it was extracted from was never replaced — the slice added a second copy instead of removing one.
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/components/forms/hooks/use-roster.ts:38` · the failure toast fires outside the request-id guard, so a superseded request that rejects toasts „Nie udało się pobrać rozliczenia pracowników" over a newer roster that loaded fine.
      test: covered by the same unit repro as the `isRosterLoading` fix.
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/components/forms/form-components/payout-roster-summary.tsx:11` · `etapNoun` welds a Polish root to an English affix — exactly the shape AGENTS.md naming rule 3 bans. Renamed `stageNoun`; the Polish forms stay in the `pluralize` argument, where they belong.
- [x] 🔵 OBSERVATION · fixed **in the gate branch only** · `impl-review`+`code-review`+`comment-noise` · `src/lib/db/kosztorys-tree.ts:134` · stray uncommitted debug leftover `// const t = () => console.log('')` — dead code, would be swept into the next commit. This branch is clean (it forked from HEAD, which never carried the line), so **the edit is still owed in the main checkout**: `git checkout -- src/lib/db/kosztorys-tree.ts` there. Flagged to the owner rather than reached across into another working tree, where a parallel agent may be mid-edit.
- [x] fixed · `comment-noise` · `src/lib/kosztorys/subcontractor-summary.ts:70` · comment restates the UNION + null-bucket rule already owned by the function JSDoc (:90) and the sort site (:141) — deleted.
- [x] fixed · `comment-noise` · `src/lib/actions/kosztorys.ts:494` · stage-autosave comment is a verbatim duplicate of the `StagePatchT` doc at `types.ts:105`, and its field mapping restates the `Partial<>` shape — trimmed to the surviving why.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/kosztorys-v2-column-opts.ts:21` · the three-item enumeration restates the three props declared on the next three lines — trimmed to the `executedValueByStage` why.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/stage-header.tsx:193` · „Conditional: mounted only while a reassignment is pending" restates the `pendingWorkerId !== undefined &&` guard on the next line — trimmed to the why.
- [x] 🔵 OBSERVATION · fixed (doc) · `impl-review` · `plan.md` Performance § · the plan claims the roster is „cached on the existing `CACHE_TAGS` for tree + transfers", but `buildKosztorysTree` is uncached anywhere in the repo, so every roster load re-materialises the tree. The plan's premise was false, not the code — code left alone (freshness is what a money dialog wants; four invalidating tags is exactly why the tree is uncached), plan corrected and the standing `[PERF] getSubcontractorRoster` line noted as the measurement hook.
      test: no automated test — a documented cost tradeoff, not a behavior; already instrumented by the perf log.
- [x] 🔵 OBSERVATION · fixed (doc) · `impl-review` · `src/lib/queries/subcontractor-roster.ts:40` · export is `getSubcontractorRoster`, plan contract said `getSubcontractorRosterForInvestment`. Module name already carries the scope, so the code keeps the shorter name and the plan was corrected to match.
- [x] fixed · `module-cohesion` · `src/lib/kosztorys/subcontractor-summary.ts:7` · 8 exports across 4 kinds after the slice grew the file 41→148 LOC — the pure settlement computation mixed with a React-key helper and a query-row name join. Split out; the compute module keeps only compute.
- [x] partially fixed / rest skipped · `module-cohesion` · `src/components/kosztorys/editor/grid/stage-header.tsx:55` · the component grew a fourth reason to change; the worker unit (state + `pickWorker` + menu section + reassign confirm) is 105 of 216 lines and arrived whole in this slice. **Landed:** the pure decision logic extracted to `stage-worker-options.ts` — which is what made the deactivated-assignee bug above testable at all, since the repo has no jsdom/RTL harness. **Skipped:** the full `stage-worker-menu.tsx` component extraction. The reassign `ConfirmDialog` is deliberately rendered *outside* `HeaderMenu` (a dialog mounted inside `DropdownMenuContent` unmounts the moment the menu closes), so lifting the unit means splitting it across two mount points — behavior-changing, and with no component-test harness there is nothing to catch a regression. Fits the gate's `skipped` bar exactly: real, but behavior-changing **and** uncertain.
- [x] 🔵 OBSERVATION · skipped · `impl-review` · `src/components/forms/expense-form/expense-form.tsx:349` · the investment listener is attached only when `needsWorker(currentType)`, so picking the investment before switching to wypłata leaves a window where the roster renders `null`. The user sees no figures rather than wrong ones — degradation, not a defect, and the two-listener design is deliberate and commented. Recorded so a future „the panel sometimes doesn't show up" report is diagnosed in one minute.
      test: no automated test — a transient render window with no incorrect output; logged to the manual-checks registry instead.
- [x] 🔵 OBSERVATION · dropped · `code-review` · `src/migrations/20260728_1_add_worker_to_kosztorys_stages.ts:14` · index/FK names diverge from Payload's drizzle convention (`kosztorys_stages_worker_id_idx` vs `kosztorys_stages_worker_idx`). Identical skew already exists in this repo's other hand-written kosztorys migrations, the migration is applied locally, and there is no functional impact — rewriting an applied migration to chase a cosmetic name is more risk than the drift.
      test: no automated test — naming cosmetics on an applied DDL statement.
- [x] dropped · `feature-first` · `src/components/forms/form-components/payout-roster-summary.tsx` · feature-first flagged it as domain code in the form-primitives tier (1 consumer vs `saldo-summary.tsx`'s 2) and proposed moving it into `expense-form/`; structure-scatter independently judged the same placement correct, because it reproduces the `use-saldo` → `saldo-summary` pairing one-for-one. Moving one half of a mirrored pair would create the inconsistency, not remove it.
- [x] dismissed · `module-cohesion` · `src/components/kosztorys/editor/use-kosztorys-editor.ts` · known deferred god module (EX-515). The slice's +50 lines are one `handleSetStageWorker` mirroring the existing `handleSetStagePlane`, a relocated `useMemo` and three passthrough returns — no new responsibility kind, so not materially worse.
- [x] 🔵 OBSERVATION · dismissed · `impl-review` · `src/components/kosztorys/editor/grid/stage-header-copy.ts` · unplanned copy extraction riding along in a feature commit. Benign and now load-bearing for the cohesion split above — the new confirm copy is long and templated, and it matches the repo's constants-in-their-own-file rule.
- [x] dismissed · `tailwind-v4-audit` · — · zero `var()`-in-brackets, zero arbitrary values, zero inline styles in the slice's new JSX; the two hits found are pre-existing, justified, and outside this diff. The slice adds no responsive classes, so the overridden 768/1024/1280 scale is not in play.
- [x] dismissed · `structure-scatter-audit` · — · no scatter introduced; every added file landed in a pre-existing home, and `stage-header-copy.ts` (the one first-of-kind file) competes with nothing — cross-cutting labels live in `lib/**/constants.ts`, a single component's strings colocate.

## Simplify pass

Ran as the gate's serial mutating step against the triaged findings — 15 applied, 0 proposed, 6 dismissed/dropped; every finding is folded into `## Findings` above (tagged with its originating source), no separate list. No standalone `/simplify` report file: the gate's own ledger is the single record, per the one-list rule.

Two dedups reached outside the reviewed diff and landed anyway (size/risk is the deferral test, not file-set):

- `roundToCents` (`src/lib/utils/round-to-cents.ts`) — new shared money-comparison helper; `reconciliation.ts` was collapsed onto it from its own inline `Math.round(x * 100)` pair.
- `isActiveRef` — `entity-combobox-field.tsx` now calls the helper the slice extracted, instead of keeping the inline `item.active !== false` copy beside it.

Three modules were split out for cohesion: `worker-key.ts`, `payout-worker-names.ts` (both out of `subcontractor-summary.ts`, leaving it compute-only) and `stage-worker-options.ts`.

## Tests & suite

- Whole-tree gate run before the gate opened: `typecheck` ✅ · `lint` ✅ (0 errors, 83 pre-existing warnings) · `test` ✅ 1900 passed / 85 skipped · `test:integration` ✅ 82 passed / 28 files · `build` ✅
- **Regression tests authored at the gate — 15 new, all red-first:**
  - `src/__tests__/components/kosztorys/editor/grid/stage-worker-options.test.ts` (new, 8 tests) — active-only filtering, a missing `active` flag, a deactivated current holder still named *and* still offered, no resurrection of other deactivated workers, `nameOf`.
  - `src/__tests__/lib/kosztorys/subcontractor-summary.test.ts` (+7) — 3 float-residue tests (`4.35 * 100 === 434.99999999999994` is the genuine repro; the obvious `1234.5599999999999` candidate collapses to exactly `0`), plus 4 under `describe('the unattributed residual is not a person')`.
  - The `use-roster.ts` fixes (stranded `isRosterLoading`, unguarded toast) carry **no automated test**: Vitest here is node-only — no jsdom, no `@testing-library/react` — so a hook cannot be rendered. Logged to the manual-checks registry; the E2E in **EX-637** is the eventual browser-level guard.
- **Post-gate run:** `typecheck` ✅ clean · `lint` ✅ 0 errors / 83 warnings (byte-identical to baseline) · `test` ✅ **1915 passed / 85 skipped, 117 files** (+15, matching the new specs).
