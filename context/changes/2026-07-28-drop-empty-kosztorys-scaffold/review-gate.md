# Review-gate ledger — drop-empty-kosztorys-scaffold (EX-615) · 2026-07-28

Scope: `6aa2d45a..HEAD` (4 commits) + working tree.
Fan-out: `/10x-impl-review`, `/code-review`, `comment-noise-audit` (flag-only), `tailwind-v4-audit`.
Dropped as inapplicable: `feature-first-structure`, `structure-scatter-audit`, `module-cohesion-audit`
— the slice adds no new files and grows none meaningfully, so all three have nothing to judge.

## Findings

- [x] 🟡 WARNING · skipped · `code-review` + `impl-review` · `src/components/kosztorys/editor/kosztorys-editor-body.tsx:203` · **The hint is occluded by the totals panel, which persists `open` by default.** Confirmed: `KosztorysTotalsPanel` is `absolute inset-x-0 bottom-0 z-20 … data-[state=open]:h-full` with an opaque `bg-background` (`kosztorys-totals-panel.tsx:39`), `useTotalsPanelOpen` seeds `'open'` (`use-totals-panel-open.ts:13`), and the hint is an un-z-indexed `absolute inset-0` sibling. So on a fresh profile the entire Phase 1 payload is invisible. **Not auto-fixed:** the obvious `z-30` would paint the hint _on top of_ the expanded panel — a worse regression. Occlusion is also internally consistent (the panel hides the grid rows too), and the toolbar's „Dodaj" menu stays reachable outside the container, so it is not a dead end. Left as a deliberate human call.
      test: no automated test (here) · e2e — pinned into **EX-617** as a named scenario, plus a manual-check row in `manual-checks.md` under `## EX-615` so a human decides the intended behaviour before it is locked in.
- [x] 🟡 WARNING · fixed · `impl-review` · `src/app/(frontend)/probe-zz/` · **Leftover probe route in the app tree breaks `next build`.** `actions-factory.ts` was `'use server'` + `export const doThing = 42` — a non-async export, which Next rejects at build time; `page.tsx` imported it, so it sat in the route graph. `tsc --noEmit` cannot see this, which is why the plan's typecheck gate passed while the build was broken. Untracked scratch from this change's investigation. Deleted.
      test: no automated test — `pnpm build` is the deterministic guard, and the file was never committed.
- [x] 🟡 WARNING · dismissed · `impl-review` · `src/components/kosztorys/editor/kosztorys-editor-v2.tsx:17` · **False positive.** Claimed an uncommitted props-spread refactor of the two editor files. `git status` is clean apart from this gate's own edits — the `{...panelData}` / `{...bodyData}` spread is committed in an earlier slice, not pending work. No provenance question exists.
- [x] 🟡 WARNING · fixed (filed) · `impl-review` · `plan.md:276-280` · The plan's mandated E2E backlog issue was never filed. Filed **EX-617** (project "Wykonczymy", label `e2e-backlog`) with the plan's verbatim scenario plus the search, client-view, „Praca"-gate and panel-open cases; id recorded in the plan's Testing Strategy.
      test: `/10x-e2e` · e2e — deferred with its disposition recorded in EX-617, per the deferred-correctness rail.
- [x] 🔵 OBSERVATION · skipped · `impl-review` · `kosztorys-editor-body.tsx:208` · The copy says „Dodaj sekcję **lub etap**" but only a sekcja clears the hint — an etap is investment-scoped and emits no rows. Verified **correct as written**: an etap-only kosztorys genuinely is empty, and etap-first is a supported flow the plan tests deliberately (Manual step 2). Narrowing the copy would hide a real affordance. Left; the feedback question rides on EX-617 and the manual pass.
      test: no automated test — copy/feedback judgement, not a logic branch; covered by the manual walkthrough.
- [x] 🔵 OBSERVATION · dismissed · `impl-review` + `code-review` · `kosztorys-editor-body.tsx:203` · `subtotals` is item-derived, so the gate means "zero item rows", not "zero sekcje" — a 0-item sekcja would show wrong-advice copy. **Unreachable:** `createSectionWithFirstItem` mints section+item in one transaction (DO4) and `planItemRemovalFromCounts` cascades the section on last-item delete (`delete-policy.ts:47-48`). The section bands are built from `viewRows`, not `tree.sections`, so such a sekcja would render nothing either way.
- [x] 🔵 OBSERVATION · dismissed · `code-review` · `use-restore-remount.ts:33` · Dropping `becamePopulated` leaves no live path needing it. Verified: the only remaining `triggerRestore` caller is `handleRestored` ← `KosztorysVersionsDrawer.onRestored`, whose `restoreKosztorys` always ends in a `payload.update` on `investments` (`restore-kosztorys.ts:28-38`), so `tree.revision` always bumps. „Sekcja z szablonu…" now goes through `handleAppendedSections`, an in-grid patch that never arms the latch.
- [x] 🔵 OBSERVATION · dismissed · `code-review` · repo-wide · No orphaned exports or imports. Each named symbol verified still live (`createSectionWithFirstItem`, `withPayloadTransaction`, `seedInvestmentFromPreset`, `logError`, `EmptyState` props); the four deleted symbols have zero references left in `src/` or `e2e/`.
- [x] fixed · `impl-review` + `code-review` · `context/foundation/manual-checks.md:30` · A stray `a` prepended to `### Phase 2: UI pre-check + block surfacing` in the epilogue commit, breaking an **unrelated** slice's heading (S-08 kosztorys-delete-guard). Removed.
- [x] fixed · `comment-noise` · `add-sections-from-preset-dialog.tsx:28` · `onAppended` comment still contrasted against "the empty-kosztorys dialog … remounts via its own path" — a deleted component. Rewritten to state what the callback is for.
- [x] fixed · `comment-noise` · `add-sections-from-preset-dialog.tsx:37` · **The trim trap, caught in the act.** The slice turned `Fetch-on-open, mirroring seed-from-preset-button.` into `The preset list is fetched on open.` — de-fanged narration of the `useEffect(…, [open])` 14 lines below, which carries its own why. Deleted the sentence, kept the multi-select rationale.
- [x] fixed · `comment-noise` · `create-section.ts:9` · Path enumeration still named "the EX-463 new-investment auto-seed" as a third caller. Trimmed to append + insert-at; the load-bearing 0-item/transaction rationale kept.
- [x] fixed · `comment-noise` · `create-investment-preset.test.ts:125`, `serialize-apply-preset.test.ts:282` · Both named the deleted „Wypełnij z szablonu" CTA. Parentheticals cut; the regression rationale in the latter kept (it is why that test exists).
- [x] fixed · `comment-noise` · `kosztorys-create-order.test.ts:6` · Header still promised "Two structure-create invariants" after CR2's block was deleted. Corrected to one.
- [x] fixed · `simplify` · `src/lib/kosztorys/seed-from-preset.ts:11` · Header described two callers and the deleted CTA, with a concurrency paragraph justifying the guard by "the CTA's disabled-while-seeding state". Rewritten to the surviving single caller.
- [x] fixed · `simplify` · `use-restore-remount.ts:23` · Hook still took the whole `KosztorysTreeT` to read one scalar (`tree.sections.length` left with `becamePopulated`). Narrowed to `useRestoreRemount(revision: string)`; `KosztorysTreeT` import dropped; call site passes `tree.revision`.
- [x] fixed · `simplify` · `kosztorys-editor-v2.tsx:17,44-54` · `investmentId` / `tree` / `investmentName` were pulled out of the rest-spread only to be written back verbatim — the half of the spread refactor that never landed. Now `props` + a local destructure + `{...props}`.
- [x] fixed · `simplify` · `kosztorys-create-order.test.ts:6,93` · Header still claimed "section counts" (a CR2 assertion) and enumerated "CR1" with one invariant left; a single-child `describe` wrapped the lone `it`. Both collapsed.
- [x] filed EX-616 · `simplify` · `kosztorys-editor-body.tsx:203` · The hint's sibling state — a search filtering every row out — still renders a bare grid with no message, the same "looks broken" screen the hint exists to prevent. Genuinely out of the slice: the `subtotals` gate is correct and the hint's copy would be wrong advice for a filtered grid, so it needs its own copy. Filed to "Wykonczymy".
- [x] dropped · `simplify` · `kosztorys-editor-body.tsx:57` · `depositTransactions = []` is the last sibling prop still defaulted-and-relayed by hand. Making it optional is a prop-contract change on `SummaryPanelContent`, whose `PropsT` argues the required-ness is deliberate — behaviour-adjacent, uncertain, two-line win.
- [x] dropped · `simplify` · `kosztorys-editor-body.tsx:204` · The overlay has no `z-index` while dsg internals sit at 10–40. Verified harmless: an empty grid occupies only the top ~120px and none of those elements is opaque at the hint's centre. (Distinct from the totals-panel finding above, which _is_ real.)
- [x] dropped · `code-review` · `kosztorys-create-order.test.ts` · Removing the CR2 block leaves the no-preset create path with no DB spec. Correct to delete (it tested a deleted action), and the surviving behaviour is "does nothing" — a spec asserting `sectionCount === 0` would be near-worthless.
- [x] dismissed · `simplify` · `seed-from-preset.ts:36` · The `'not-empty'` branch is now unreachable in production. Kept: one in-transaction `SELECT 1 … LIMIT 1`, still exercised by `serialize-apply-preset.test.ts`, and deleting it would trade a cheap invariant for a silently-destructive path if a second caller appears.
- [x] dismissed · `simplify` · `kosztorys-editor-body.tsx:61` · Suspected the rest-spread would bust the JSX element memo on a 1000-row grid. Ran the repo's own `babel-plugin-react-compiler` over the shape: the rest element is cached on props-object identity. Efficiency-neutral versus the named relay it replaced.
- [x] dismissed · `simplify` · `src/components/ui/empty-state.tsx` · `EmptyState`'s base `flex-1` is inert once the caller makes it `absolute inset-0`. Changes nothing rendered; not worth touching a shared primitive.
- [x] dismissed · `tailwind-v4-audit` · `kosztorys-editor-body.tsx:203-210` · Clean. No `var(--token)` in brackets, no inline `style`, no arbitrary values in the added JSX. `EmptyState` merges `className` via `cn` (`twMerge(clsx())`) with no position/inset conflict, and the parent at `:171` carries `relative`, so `absolute inset-0` anchors correctly and centres.
- [x] dismissed · `reuse-scan` · diff-wide · 0 findings. The overlay calls the shared `ui/empty-state.tsx` (same primitive as `error.tsx` / `not-found.tsx`); no intra-diff repetition; the deletions left no duplicated path (`applyPreset` / `appendPresetSections` both layer on `insert-kosztorys-tree.ts`, split documented as deliberate at `append-preset-sections.ts:17`).
- [x] dismissed · `comment-noise` · `use-kosztorys-editor.ts:151` · `the deleted toClientView used to hold` — textbook vanished-state, but from a **prior** slice, not this diff. Out of scope; noted so it isn't lost.

## Simplify pass

Ran `/simplify` (+ `primitive-reuse-scan`) — 5 applied, 1 filed (EX-616), 2 dropped, 4 dismissed; reuse-scan 0 findings. Each folded into `## Findings` above tagged `simplify` / `reuse-scan`.
Report: `/var/folders/cf/bs0zn0gj1lgbc2n7ps0z211h0000gn/T/simplify-XXXXXX.Q49WXkEQs9.md`

## Tests & suite

No new automated tests owed: the slice is subtractive, the state it newly exposes (zero sekcje) was
already reachable via `removeSectionAction` and probed clean across the financial pipeline, and the
one new surface is a presentational branch. The browser-level E2E is deferred and **filed as EX-617**.

- `pnpm typecheck` — pass
- `pnpm lint` — pass (0 errors; 85 pre-existing warnings, all in `src/migrations/*` and unrelated files)
- `pnpm test` — pass (1826 passed, 67 skipped / 108 files passed, 24 skipped — the skips are the DB-gated specs)
- `pnpm build` — pass; the route manifest no longer carries `/probe-zz`, so the `'use server'` break is gone
- `pnpm test:e2e` — not run (user's call: the e2e leg needs the 5435 `db-test` container)
