# Review-gate ledger — staging, 2026-08-26 (four changes, 19 commits)

Scope — every commit landed on `staging` today, across four changes:

| Change                    | Commits                                                                      |
| ------------------------- | ---------------------------------------------------------------------------- |
| `fleet-costs-window`      | `c6a56921` `64f5cf5d` `d0c81b13` `d5361582`                                  |
| `kosztorys-stage-import`  | `16e32c97` `06bf1b04` `6ec390a9`                                             |
| `table-column-reordering` | `bef2cffe` `ff1352d1` `88cbb781` `be59be6a` `e040b8fe` `0c69995f` `ad7eb14f` |
| `notification-recipients` | `cb9b878a` `86b3240a` `44fb9ab7` `5b3c5836` `46b29f71`                       |

**Protected — do not touch.** 34 paths are dirty from `fleet-sheet-parity`, still in flight in this
shared tree (its own `review-gate.md` is open). No mutating pass may edit them; the snapshot is
`/tmp/dirty.txt`. `context/foundation/manual-checks.md` is in that set — this gate's own section is
written there but stays uncommitted for the same reason.

## Findings

<!-- [box] [severity, bug-finding checks only] · disposition · `source` · `file:line` · what — reason -->

### Correctness

- [x] 🔴 CRITICAL · fixed · `table-column-reordering` · `src/components/filters/column-toggle.tsx` · base ranks were derived from `table.getAllLeafColumns()`, which TanStack returns already permuted by `columnOrder`. A rank is a midpoint between its new neighbours', and an unranked neighbour falls back to its base — so the SECOND drag measured against neighbours that had already moved and landed one slot off. `baseRanks` now flows from `DataTable`'s declared column list through the toolbar context; `leafColumnIds` extracted to `src/lib/table/leaf-column-ids.ts` so the declared order is derivable without a renderer. Same rule the kosztorys grid's `assembleBaseRanks` already followed.
      test: test-driven-debugging · unit — `src/__tests__/lib/table/leaf-column-ids.test.ts` (5 tests). Verified by mutation: reverting to the ordered list makes „lands a second drop where it was dropped" fail.
- [x] 🔴 CRITICAL · fixed · `fleet-costs-window` · `src/components/fleet/inspection-history.tsx:14` · imports `formatKmOrDash`, which HEAD's `format-distance.ts` did not export — the helper existed only in a parallel agent's uncommitted copy, so a clean checkout of `staging` failed typecheck with TS2305. Fixed in `202fe790` by committing the export (the file's entire uncommitted diff was exactly that one helper, so nothing of the other agent's work was swept in).
      test: no automated test — a missing export is a compile error; `pnpm typecheck` on a clean tree is the guard, and the reason it did not fire is that the dirty helper was present. No runtime behaviour to assert.
- [x] 🟡 WARNING · fixed · `fleet-costs-window` · `src/components/fleet/inspection-history.tsx`, `src/components/fleet/vehicle-costs.tsx` · empty-state wording picked „w wybranym okresie" from `hasWindow` alone, so a section empty in the FULL history still blamed the window — the exact failure `narrowHistory`'s own docstring says must not happen. The rule now lives once, in `emptyHistoryLabel` beside `narrowHistory`; both components take the unnarrowed history and answer per section (per type for the przeglądy list, per costed set for the koszty table, so odometer rows cannot make an empty window look like a hiding one).
      test: TDD · unit — `emptyHistoryLabel` block in `src/__tests__/lib/fleet/history-window.test.ts`.
- [x] 🟡 WARNING · fixed · `kosztorys-stage-import` · `src/components/kosztorys/editor/dialogs/sheet-import-dialog.tsx` · the editor mounts the dialog once for both triggers, so the rozliczenie pick survived „Anuluj" and a failed apply and would stamp every etap of the NEXT import. Reset on open, adjust-state-during-render (no `useEffect`, per the repo rule).
      test: no automated test — the state lives in a dialog with no hook-renderer harness in this repo; the reset is three lines whose whole risk is the mount lifecycle, which only the browser exercises. Covered by the E2E filed below.
- [x] 🟡 WARNING · fixed · `kosztorys-stage-import` · `src/lib/kosztorys/sheet-import/parse-labor-tab.ts` · an etap was labelled by its position in the surviving run, so on a sheet where the owner deleted an etap column the run's third column was labelled „Etap 3" while the sheet calls it etap 9. The label now reads the number out of the sheet's own caption (`sheetStageNumber`), falling back to the position only for a column the owner blanked.
      test: test-driven-debugging · unit — `RENUMBERED_CAPTIONS` fixture in `parse-labor-tab.test.ts`. The first attempt was tautological (both real fixtures run captions 1..10, so position and sheet number coincide); the fixture rewrites column F's caption to „9 etap ilosc" so the two part company. Verified by mutation: 2 of 2 fail against the old code.
- [x] 🟡 WARNING · fixed · `notification-recipients` · `src/lib/actions/notification-recipients.ts` · the action re-implemented the form's two rules with its own `zod` shape and its own message strings. Now `validateAction(recipientEmailsSchema, …)` against the single schema in `recipient-list-schema.ts`, which also dedupes — one delivery per person, and the card can key its rows by address.
      test: TDD · integration — „stores one entry per address when the same one is typed twice" and „rejects a MANAGER without touching the row" in `src/__tests__/lib/actions/notification-recipients.test.ts` (runs under `pnpm test:integration`).
- [x] 🟡 WARNING · fixed · `notification-recipients` · `src/lib/fleet/notify.ts` · the fleet sender had no spec at all — neither that it addresses the whole list in ONE message (the caller stamps the deadlines as announced once), nor that it throws rather than mailing the void.
      test: TDD · unit — new `src/__tests__/lib/fleet/notify.test.ts` (2 tests).
- [x] 🔵 OBSERVATION · fixed · `fleet-costs-window` · `src/components/filters/date-filters.tsx` · a second, UNVALIDATED URL→`DateRangeT` construction beside the validated `parseDateRange` the same page's server half uses, so `/flota?from=abc` diverged between the two — and the picker anchors `new Date(from + 'T00:00:00')`, rendering „NaN" into Rok/Miesiąc. The shape check now lives once as `dayBound` in `date-range.ts`; `parseDateRange` delegates to it.
      test: no new test — `src/__tests__/lib/utils/parse-date-range.test.ts` already asserts the rule; the fix moved the rule under it rather than adding one.
- [x] 🔵 OBSERVATION · dismissed · `notification-recipients` · `src/lib/actions/notification-recipients.ts` · read-modify-write fails naming a list the owner never touched if a SIBLING list is empty. Unreachable: `required: true` + `minRows: 1` on every list makes an empty sibling impossible through any write path, and the seed migration creates all three.
- [x] 🔵 OBSERVATION · dismissed · `notification-recipients` · `src/lib/leads/notify.ts:52` · an empty `newLead` list would permanently fail a lead. It cannot be empty (above); and if it were, `capture-lead.ts:71` retries three times and flips `notifyStatus: failed` — the lead itself is already persisted, so nothing is lost.
- [x] 🔵 OBSERVATION · dismissed · `fleet-costs-window` · `src/components/tables/fleet.tsx:65-74` · the deadline sort accessor returning `undefined` for an exempt deadline is an unrecorded behaviour change. The behaviour is correct and the rationale is already in the code („bezterminowo" outranks the stale event on file); what was missing was a record, which this ledger now is.
- [x] 🔵 OBSERVATION · dismissed · `fleet-costs-window` · `src/components/fleet/inspection-history.tsx:31-39` · `columnsFor` reads the WINDOWED entries, so columns appear and vanish as the window moves. That is the documented rule („an always-present column of dashes costs width and says nothing") applied to what the user is actually looking at — the window IS the view.
- [x] 🔵 OBSERVATION · dropped · `fleet-costs-window` · `src/components/filters/date-range-picker.tsx:71-80` · an inverted window (Od later than Do) matches nothing and every section reports missing data. Pre-existing in the picker, self-evident to whoever typed it, and self-correcting; not worth a guard.
- [x] 🟡 WARNING · dropped · `kosztorys-stage-import` · `src/lib/kosztorys/sheet-import/parse-labor-tab.ts:206` · a caption row that repeated the block header would name every surviving etap identically. `STAGE_MARKER` („wykonano") is matched on row 2 and captions are read off row 3, which the two real sheets fill with numbered captions and which the documented variation renames to crew names — a legitimate name. No sheet has the colliding shape.
- [x] 🟡 WARNING · skipped · `kosztorys-stage-import` · `src/lib/kosztorys/sheet-import/parse-labor-tab.ts:45` · `number()`'s `|| 0` widens a non-numeric cell to zero. Behaviour-changing and genuinely ambiguous for the shapes that reach it (`"1,234"` is a thousands separator in one sheet and a decimal comma in another) — needs the sheet inspected before deciding, not a review-gate edit.
- [x] 🔵 OBSERVATION · skipped · `table-column-reordering` · `src/components/fleet/fleet-data-table.tsx:42-49` · positional footer `colSpan` against `visibleColumnIds`. The file is in the protected `fleet-sheet-parity` set — it belongs to that change's own gate, not this one.

### Cleanups

- [x] fixed · `table-column-reordering` · `src/lib/table/column-order.ts` · `dropNonFiniteRanks` was written twice (storage layer and the kosztorys `use-column-order` hook's `withFiniteRanks`). Deduped into `column-order.ts`, preserving identity when nothing is dropped — the kosztorys store runs it every render.
- [x] fixed · `table-column-reordering` · `src/lib/table/column-prefs-storage.ts` · `readVisibility` returned whatever `JSON.parse` produced; a stored `null` parses fine and then white-screens the table at `columnVisibility[id] !== false`. Shape-checked, non-boolean entries dropped.
      test: test-driven-debugging · unit — two cases in `column-prefs-storage.test.ts`.
- [x] fixed · `table-column-reordering` · `src/lib/table/column-prefs-storage.ts` · the two write helpers were byte-identical swallows; extracted `persist`. (Deleting their `catch` comments in the comment pass had left `catch {}` — an eslint `no-empty` error; the extraction fixes both at one site.)
- [x] fixed · placement · `src/lib/table/` · `column-prefs-storage.ts` + spec moved out of `components/ui/data-table/` — no React in it, and `leafColumnIds` had to be testable without a renderer.
- [x] fixed · placement · `fc9d4db7` · `column-toggle-order.test.ts` → `__tests__/lib/table/`, `components/notifications/` → `components/notification-recipients/`.
- [x] fixed · `fleet-costs-window` · `date-range-picker.tsx:37,42,82`, `vehicle-detail-tabs.tsx:33`, `history-window.test.ts` · five bare `{}` where `ALL_TIME` exists and documents itself as the named form.
- [x] fixed · comment-noise · `src/lib/env/schema.ts:73` · vanished-state narration („recipients moved to the global") trimmed; kept the half that says why a _from_-address is not a recipient.
- [x] fixed · comment-noise · `src/migrations/20260826_0_notification_recipients.ts:4-5` · restated the migration's own filename plus the env vars it replaces. Deleted; the rest of the header carries real rationale.
- [x] fixed · comment-noise · `src/components/kosztorys/editor/dialogs/sheet-import-dialog.tsx` · `PlaneBlock`'s docstring restated the `verdict` string rendered three lines below it. Trimmed to the part the UI does not say.
- [x] fixed · comment-noise · `src/lib/actions/kosztorys-import.ts:276` · the `plane` param comment opened by restating the parameter name and type. Trimmed to the „nonsense is read as nie ustawiaj" rule.
- [x] fixed · comment-noise · `src/components/ui/column-toggle-menu.tsx` · „When passed, the picker also offers the reorder trigger" — restates an optional prop.
- [x] fixed · docs · `context/foundation/manual-checks.md` · five lines still sent the reader to the deleted `LEADS_ALERT_EMAIL` / `LEADS_NOTIFY_EMAIL` env vars; now they name the lists („Alerty techniczne", „Powiadomienia o nowych zgłoszeniach"). **Uncommitted** — the file is in the protected set.
- [x] fixed · `kosztorys-stage-import` · `src/__tests__/lib/actions/kosztorys-import.test.ts:161` · comment claimed „the action takes no plan by design" after the second parameter became `plane`. The assertion is still right; the wording was not.
- [x] dropped · `fleet-costs-window` · `src/components/tables/fleet.tsx:36-40` · two column ids differing only in case (`tyres` / `TYRES`). They are different concepts (the tyre set on the car vs the swap deadline), the type system keeps them apart, and the comment above them already flags the confusability. Renaming means a migration on a column that just shipped.
- [x] dropped · `fleet-costs-window` · `src/components/tables/fleet.tsx:36` · „Opony" inserted mid-list rather than appended, shifting the assemble-index space a persisted order ranks against. Self-correcting via „Kolumny → Przywróć".
- [x] dropped · `table-column-reordering` · `src/components/transfers/transfer-data-table.tsx:59` · one `storageKey="transfers"` across three views whose `excludeColumns` differ, so a rank set in one is midpointed against a different neighbour set in another. Splitting the key resets everyone's saved layout and stops „hide Inwestycja" carrying between the views, which is arguably the wanted behaviour; the ordering artefact costs one drag.
- [x] dropped · `table-column-reordering` · `src/components/ui/data-table/data-table.tsx:96` · `columnOrder` is a fresh array each render. The compiler memo is keyed on `columns`, which every caller rebuilds per render anyway, so fixing it means memoising `getTransferColumns` and friends upstream — disproportionate for paginated tables.
- [x] dropped · `notification-recipients` · `src/lib/queries/notification-recipients.ts` · `unstable_cache` with tag invalidation and no TTL, so a write that bypasses the action never expires the page. `admin: { hidden: true }` removes the second editor; only a hand-rolled REST call reaches it.
- [x] dismissed · `notification-recipients` · `src/migrations/20260826_0_notification_recipients.ts` · seed addresses hardcoded rather than read from env. Owner's explicit call: the addresses are identical on every environment, and `findGlobal` on an unseeded global returns defaults, so `minRows: 1` could never fire on a row that was never created.
- [x] dropped · `notification-recipients` · `src/globals/notification-recipients.ts` · `hidden: true` still leaves the global writable over REST for an ADMIN/OWNER, bypassing the action's validation. `required` + `minRows` enforce the core rule structurally on every write path, so what a REST write skips is the cache invalidation only.

### Owed later

- [x] deferred · `notification-recipients` · browser E2E for the two recipient cards (`/flota`, `/zgloszenia`): add, remove, refuse-empty, dedupe, the MANAGER read-but-not-save split, and the „nie rusza pozostałych list" isolation — filed **EX-741** (`e2e-backlog`, project „Wykonczymy").
- [x] deferred · `kosztorys-stage-import` · browser E2E for the import dialog's rozliczenie pick surviving „Anuluj" — the one leg no unit test reaches (see the fixed 🟡 above), because the risk IS the mount lifecycle of a dialog that never unmounts — filed **EX-742** (`e2e-backlog`, project „Wykonczymy").

## Simplify pass

Folded into `## Findings` above (the `Cleanups` section) rather than kept as a second list — every
mutating edit this gate applied is one checkbox there.

## Tests & suite

- `pnpm typecheck` — clean.
- `pnpm test` — 2903 passed, 170 skipped (the 50 DB-backed files, which run under `pnpm test:integration`).
- `pnpm lint` — clean on every tracked file. The one remaining error is `test.js` at the repo root:
  untracked scratch, not part of any change here.
- `pnpm test:integration` / `pnpm test:parity` / `pnpm test:e2e` / `pnpm build` — not run; the shared
  test DB and the dev tree belong to `fleet-sheet-parity` while it is in flight.
