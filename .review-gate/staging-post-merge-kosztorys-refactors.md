# Review-gate ledger — staging post-merge kosztorys refactors · 2026-07-25

Scope: 12 commits since merge `2fed8bf3` (`d5b80d37`..`8ef4a3e5`) — the grid-primitive
extraction batch (`EditableCellInput` / `ReadOnlyCellText` / `HeaderLabel` / `useInlineRename`),
the stage-plane single-sourcing, the column-width unification, the seed-etap fixes, and the
`StagePlaneT` + `CostVariantT` → `ToolPlaneT` union merge. 29 files, kosztorys editor.
Working tree clean at triage time.

No `plan.md` covers this batch (post-merge refactor run directly on `staging`) →
`/10x-impl-review` dropped from the fan-out. Ran: `/code-review`, `tailwind-v4-audit`,
`feature-first-structure`, `module-cohesion-audit`, `structure-scatter-audit`,
`comment-noise-audit`.

## Findings

<!-- ONE checkbox per finding — every source folds in here. Most-severe first.
     Format: [box] [severity tag, bug-finding checks only] · disposition · `source` · `file:line` · what — reason -->

- [x] 🟡 WARNING · fixed · `code-review` · `src/components/kosztorys/editor/grid/sort-header.tsx:25` · The active-sort **bold** indicator is silently lost. The label used to be a bare `<span>{label}</span>` that inherited `font-semibold` from `triggerClassName`; `HeaderLabel` now sets `font-medium` **on the label element**, and a class on the element beats inheritance (tailwind-merge only merges within one class string, not across two elements). Sorted columns render at weight 500 instead of 600 — distinguished only by the primary tint and the arrow glyph. Verified against `git show 2fed8bf3:…/sort-header.tsx`.
      test: no automated test · unit — a rendered-class assertion; guarded instead by making the weight caller-supplied so there is no cross-element inheritance left to break.
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/components/kosztorys/editor/grid/cells/read-only-cell-text.tsx:9` · The `ReadOnlyCellText` adoption changed computed + subcontractor cells from `block w-full` to `block size-full`, i.e. it added `height:100%`. `.dsg-cell` is `display:flex; align-items:center` (`react-datasheet-grid/dist/style.css:58-60`) with `rowHeight={32}`, so a 100%-height child defeats the centering and the text sits at the top of the span — while the sibling `<input class="size-full">` still vertically centers its value. Every computed money column's figure shifts ~6px up and misaligns against the editable cells in the same row. Also affects the four pre-existing `ReadOnlyCellText` call sites, so the fix is a net repair, not just a revert.
      test: no automated test · unit — pure CSS geometry inside a third-party grid; a jsdom class assertion would pin the implementation without proving the visual result. Belongs in the browser pass (see Tests & suite).
- [x] fixed · `structure-scatter` + `feature-first` · `src/components/kosztorys/editor/use-inline-rename.ts` → `editor/hooks/` · New leaf hook landed at the `editor/` root while `editor/hooks/` already holds 12 sibling leaf hooks. The two pre-existing root hooks are the aggregate root (`use-kosztorys-editor.ts`) and its context — a rule this hook breaks, since nothing composes it and both consumers are grid components. Both audits flagged it independently. 3 import sites, not the 2 both audits reported.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/cells/editable-cell-input.tsx:12` · **Delete whole comment** — "counterpart to ReadOnlyCellText" (the sibling file says it) + "shared so they can't drift" (the import says it) + "the way their read-only halves already had once" (vanished-state, the stray class is gone). Nothing survives the strip test.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/header-label.tsx:4` · **Delete whole comment** — `HeaderLabel` returning `text-xs font-medium` _is_ the comment; "single source of truth so a change isn't repeated" is the definition of extracting a component, and the consumer enumeration rots by construction.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/use-inline-rename.ts:3` · **Trim** — sentence 1 narrates the hook body; the closing clause ("the two call sites used to disagree here, Escape in the stage header committing the unchanged label") is vanished-state describing a bug this extraction deleted. Keep only the `cancelledRef`-survives-the-blur rationale.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/cells/read-only-cell-text.tsx:4` · **Trim** — "plain left-aligned, truncated label" restates `truncate text-left`; "shared so the four custom cells can't drift (they had before — one carried a stray alignment class)" is extraction-restatement + vanished-state. Keep only the compressed `muted` rule; drop "same markup, just greyed…" which narrates `text-muted-foreground`.
- [x] fixed · `comment-noise` · `src/lib/actions/kosztorys.ts:21` · **Trim** — cut "the two used to be five separate literal lists" (vanished-state). The derivation rationale before the em dash binds a future editor and stays.
- [x] fixed · `comment-noise` · `src/lib/kosztorys/constants.ts:3` · **Trim** — "in pick order" is load-bearing (array order drives picker display order, not inferable from the const); the rest is extraction restatement plus a consumer list that will rot.
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/stage-header.tsx:30` · **Trim** — "Stage column header" restates `StageHeader`; "„Usuń etap" deletes behind a confirm" narrates the visible `ConfirmDialog`. Keep only the empty-label→`null`→„Etap N" round-trip, which lives in the action, not this file.
- [x] fixed · `comment-noise` · `src/lib/kosztorys/seed-blank.ts:9` · **Trim (minor)** — only the opening locating clause narrates the function and its body; the EX-463 rationale and the no-etap justification stay.
- [x] fixed · `comment-noise` · `src/lib/kosztorys/types.ts:86` · **Trim** — the `ToolPlaneT` doc is load-bearing, but the closing flourish "— the state the TriangleAlert warning screams about" couples a domain type to one UI component and is purple.
- [x] fixed · `comment-noise` · `src/lib/kosztorys/types.ts:4` · **Trim** — "in S-01 it is carried as 0 (VAT arrives in S-12)" is stale slice scaffolding; `vatRate` is live and `DEFAULT_VAT` exists. Flagged as out-of-scope by the audit, but it's a one-line vanished-state comment in a file this diff edits — adjacency is not a reason to leave it.
- [x] fixed · `feature-first` vs `structure-scatter` · `header-label.tsx`, `editable-cell-input.tsx`, `read-only-cell-text.tsx` → `src/components/ui/datasheet-grid/` · The two placement audits disagreed on whether these are grid-library chrome (shared tier) or feature-local cells. **User ruled: `ui/datasheet-grid`.** All three encode react-datasheet-grid facts and no kosztorys domain — width vs `.dsg-cell`'s flex centring, `bg-transparent` against dsg's cell borders, Enter/Escape swallowing against its selection layer. Moved with `git mv`; 8 import sites updated. `grid/cells/` is now domain cells only, and `HeaderMenu`'s `label` contract no longer has to be satisfied from a feature folder.
- [x] fixed · `code-review` + `simplify` · `src/components/kosztorys/editor/grid/kosztorys-v2-columns.tsx:172` · The `Math.max(col.minWidth ?? 0, 140)` floor was a clamp, not a default — it made every declared `minWidth < 140` unreachable (`unit` 64, `discountType` 80, `discountValue` 110, both subcontractor columns 90, `price` 90). Originally dropped as cosmetic; **user overrode**. Fixed at the mechanism: `col.minWidth ?? DEFAULT_COLUMN_MIN_WIDTH`, with the seven values that only restated the default deleted and `layerGapColumn`'s deliberate 24 now honoured. Three of the four simplify agents independently reached the same fix; two of them claimed the floor also regressed the trailing gap to 140px — **not reproduced**: dsg freezes `factor === 0` items before the min/max violation loop, so it held at 48px either way. The `??` form is correct under both readings.
- [x] dismissed · `code-review` · `read-only-cell-text.tsx:9` (`truncate`, `text-sm`) · The same adoption also added `truncate` and `text-sm` to computed cells. Intentional consistency, not drift: `ReadOnlyCellText` already carried both for the four disabled cells, ellipsizing beats overflowing into the neighbouring column, and the same batch raised the column-width floor to 140px so truncation is far less reachable. Only the `h-full` half (above) is a defect.
- [x] dismissed · `tailwind-v4` · `src/styles/globals.css:257-259` · New `.kosztorys-grid .dsg-cell-header { font-size: var(--text-xs) }` asserts the same value `HeaderLabel` bakes in. Not a v4 violation (reading a theme token in a stylesheet is correct; the anti-pattern is `text-[var(--text-xs)]` in JSX, which does not occur) and not dead — the CSS rule reaches dsg's vendor header chrome that `HeaderLabel` doesn't wrap. Both are needed.
- [x] dismissed · `tailwind-v4` · `src/components/kosztorys/editor/grid/stage-header.tsx:54` · `className="min-w-0 px-1 text-xs"` overrides the primitive's `text-sm`/`px-2`. Legal utilities, tw-merge resolves correctly — a header-context caller against a body-cell-tuned default. Becomes a `size` variant only if more header callers appear.
- [x] dismissed · `tailwind-v4` · repo-wide · No Tailwind-aware ESLint plugin (`prettier-plugin-tailwindcss` sorts classes only), so these patterns are invisible to CI. Real gap, but wiring `eslint-plugin-better-tailwindcss` is repo-infra work with nothing to do with this diff.
- [x] dismissed · `module-cohesion` · diff-wide · **Zero cohesion findings from this diff — it made three files more cohesive.** `plane-icons.tsx` shed `PLANE_LABELS` to `constants.ts` (now one export, one concern); `stage-header.tsx` shed its inline-rename state machine and hand-rolled input; the `StagePlaneT`/`CostVariantT` merge gave the plane concept one home and collapsed five duplicated `z.enum` literals onto one. `read-only-cell-text.tsx` checked specifically for kitchen-sink drift — 29 LOC, one export, a named variant plus a `className` escape hatch. Not a grab-bag.
- [x] dismissed · `module-cohesion` · `src/components/kosztorys/editor/use-kosztorys-editor.ts` · 1118 LOC god module, but this diff's only contact is the 6-line `ToolPlaneT` rename — no new state slice, no new handler, no substantive growth. The **EX-515** deferral ("cohesive stateful unit, needs a test harness first") stands and this diff gives no new reason to revisit it.
- [x] dropped · `module-cohesion` · `src/components/kosztorys/editor/grid/kosztorys-v2-columns.tsx` · 597 LOC god-by-size (`assembleV2Columns` is a ~250-line single function). Pre-existing; this diff's contact was cosmetic and **improving** (three inline header spans replaced by the shared primitive). The proposed split into `grid/columns/*` is medium-cost — the builders share a wide opts type and four local helpers, and column _ordering_ is positional and load-bearing — so it's a review-worthy refactor, not gate churn.
- [x] dropped · `module-cohesion` · `src/lib/actions/kosztorys.ts` · ~20 server actions across four entities (items / sections / stages / investment settings) behind one filename. This diff **improved** it (the shared `stagePlaneSchema` killed five duplicated literal unions). Splitting means per-file `'use server'` directives and a barrel that must not break the action manifest — not worth it below ~700 LOC.
- [x] dropped · `structure-scatter` · `grid/cells/editable-cell-input.tsx` · The primitive lives in `cells/` but `grid/stage-header.tsx` (a header, not a cell) imports it — a naming tension inside one subtree, no competing home created. Subsumed by the open placement question above; not worth separate churn.
- [x] dropped · `structure-scatter` · `src/lib/kosztorys/` · 36 flat `.ts` files, zero subdirs — the repo's clearest subdivide-in-place candidate. Single-kind, so not scatter, and **pre-existing**: this diff modified 6 files there and added none.
- [x] dropped · `comment-noise` · `src/lib/kosztorys/seed-from-preset.ts:38` + `seed-blank.ts:11` · The "a seeded etap could only guess its plane / a guessed plane reads as confirmed" rationale is near-verbatim in both seed files. Load-bearing at both sites, so not a strip-test failure — deduping to one canonical statement plus a pointer trades a self-contained comment for an indirection. Not worth it for two files.
- [x] dismissed · `comment-noise` · `use-inline-rename.ts:22`, `editable-cell-input.tsx:4`, `section-name-cell.tsx:6,27`, `kosztorys-v2-columns.tsx:169` · Flagged borderline, inspected, kept. Each encodes a constraint the code can't state: `onEnter`/`onEscape` aren't DOM input props so the coupling to `EditableCellInput` is invisible; dsg selection swallowing; denormalized-name fan-out; external-rename staleness; the dsg minWidth-clamp-on-overflow behind the `140` floor.
- [x] dismissed · `feature-first` · `src/lib/kosztorys/{constants,types}.ts` · The tier crossings in this diff are all **correct**: `PLANE_LABELS` moved component-tree → domain tier (out of the icon renderer, next to the const it's keyed by), `TOOL_PLANES` and `ToolPlaneT` stayed in the domain tier with values in `constants.ts` and types in `types.ts`. Nothing domain-aware leaked into `components/ui/`. (Noted: the union is spelled twice — deriving `ToolPlaneT` from `TOOL_PLANES` would single-source it but costs the type its home in `types.ts`; judgment call, not a defect.)
- [x] dismissed · `code-review` · diff-wide · **No CRITICAL findings.** Explicitly verified clean: the inline-rename commit path (Enter routes through `blur()` so `onBlur` is the single commit site; no stale closure; Escape now correctly skips the commit instead of the stage header's old write-back of the unchanged label); `EditableCellInput` swallows Enter/Escape _only_ when a handler is passed, so the three handler-less callers still reach dsg exactly as before; `{...props}` spreads last so no caller prop is clobbered; the `ToolPlaneT` merge erases no live distinction (`costVariant`/`defaultCostVariant` are inert in calc/settlement, which key off `view`); no `PLANE_LABELS` collision (`deposits-table.tsx` now imports `VAT_PLANE_LABELS` from a different module); the seed condition is neither inverted nor off-by-one, and zero stages is safe downstream (every `stages.length` read is a guard).

## Simplify pass

Ran `/simplify` — 4 agents (reuse · simplification · efficiency · altitude). 7 fixed, 2 skipped (parallel-session hold), 5 dropped, 4 dismissed, 1 deferred+filed. Findings folded in below.

### Simplify pass findings (4 agents: reuse · simplification · efficiency · altitude)

- [x] fixed · `simplify` · `src/lib/kosztorys/calc.ts:36,38,43` + `grid/cells/subcontractor-columns.tsx:21,38,82,121` · `'w_tools' | 'own_tools'` still hand-spelled seven times after the `ToolPlaneT` merge — a third plane would compile clean and fail at runtime in the price columns. Flagged independently by reuse, simplification and altitude. `PriceViewT` is now `'client' | ToolPlaneT`.
- [x] fixed · `simplify` · `toolbar/kosztorys-view-axis-options.tsx:16-17` + `hooks/use-price-view.ts:9` · The last hand-typed plane-label pair and the last literal `['client','w_tools','own_tools']`, both surviving the `PLANE_LABELS` move. Now derived from `TOOL_PLANES` / `PLANE_LABELS`.
- [x] fixed · `simplify` · `grid/sort-header.tsx:29` · Dead `font-semibold` on `triggerClassName` — inert once the weight moved onto the label element, and a live contradiction of the comment two lines above. Flagged by reuse and altitude.
- [x] fixed · `simplify` · `src/lib/kosztorys/seed-blank.ts:9` · The comment trim left the first line overrunning mid-sentence; reflowed.
- [x] fixed · `simplify` · `ui/datasheet-grid/header-label.tsx` vs `src/styles/globals.css:257-259` · Two homes for header typography — the CSS rule set `font-size` on `.dsg-cell-header`, `HeaderLabel` set `text-xs font-medium` on the element inside it. **User ruled: keep the component.** Verified the rule was redundant before deleting it: every text-bearing `title:` routes through `HeaderLabel` (`title()`, `stageValueHeader`, `actionColumn`, `StageHeader`, `SortHeader`); the only non-`HeaderLabel` titles are `layerGap`'s empty `<span />` and `ResizableHeader`'s wrapper, neither of which carries text, and the stage rename input carries its own `text-xs`. Step 1 had dismissed this pair as both-needed — that was wrong, and the trace is what settled it.
- [x] skipped · `simplify` · `hooks/use-inline-rename.ts:27-31` + `grid/cells/editable-cell-input.tsx:8` · The hook returns `onEnter`/`onEscape`, which only `EditableCellInput` understands, so `kosztorys-section-row.tsx:32` has to destructure the abstraction apart and re-dispatch through `enterEscapeKeyDown` by hand. Simplification and altitude both propose the hook return a native `onKeyDown` built from the already-extracted helper, collapsing three statements of the Enter/Escape contract to one. Real and correct, but it reshapes a hook and a primitive across four files **that the parallel session is actively editing** — the one legitimate file-based hold in this gate. Worth doing as its own change.
- [x] skipped · `simplify` · `hooks/use-inline-rename.ts:6-8` · `editing` is derivable from `draft` (`draft: string | null`, `editing = draft !== null`), which also removes the impossible stale-draft state and the ternary + comment at `section-name-cell.tsx:28`. Same hold as above — same file, same parallel session. `cancelledRef` and the Enter-routes-through-blur design were **verified correct and explicitly kept** by both agents.
- [x] dropped · `simplify` · `grid/cells/editable-cell-input.tsx:1` · `forwardRef` is dead — six call sites, zero pass a `ref`, and on React 19 `ComponentProps<'input'>` already carries `ref` through the spread. Real but cosmetic; folded into the skipped hook/primitive change above rather than churned alone.
- [x] dropped · `simplify` · `grid/cells/read-only-cell-text.tsx:10` · `muted` vs the `className` passthrough are two spellings of one thing, and `computed-cell.tsx:35` already uses the raw class for the larger group. Two call sites; `muted` names a domain state (derived, not yours to type). Not worth the churn either way.
- [x] dropped · `simplify` · `grid/stage-header.tsx:53` · `className="min-w-0 px-1 text-xs"` exists only to undo `EditableCellInput`'s `px-2 text-sm` — altitude wants a `cell`/`header` context variant. One caller; already dismissed at the same line by `tailwind-v4` in Step 1. Revisit if a second header caller lands.
- [x] dropped · `simplify` · `components/ui/button.tsx:18` · `outlineDestructive` restates `outline`'s three base utilities for one call site, and the neighbouring `ghostDestructive` has zero call sites repo-wide (pre-existing). **Parallel session's file — not mine to touch.** The `align` variant in the same file was checked and is a genuine win; explicitly leave it.
- [x] dropped · `simplify` · three `MoneyAxisT` label pairs (`kosztorys-view-axis-options.tsx:32`, `grid/money-axis-toggle.tsx:11`, `summary/kosztorys-totals-panel.tsx:49`) · `{net: 'Netto', gross: 'Brutto'}` written three times — the exact analogue of the `VAT_PLANE_LABELS` extraction this batch performed one type over. Real, but each array keeps a distinct third entry, so the shared part is two words. Below the churn threshold.
- [x] deferred+filed · `simplify` · `costVariant` / `defaultCostVariant` (schema) · Altitude traced every reference: both columns are written, read back, and plumbed onto every row object, but **nothing downstream branches on either** — v1 vestiges from before stages became the settlement source of truth (EX-489). The `ToolPlaneT` merge makes it worse: three dead fields now share a name with the one live carrier, so they read as load-bearing. Schema deletion + hand-written migration → its own review. Cheap under the throwaway-data rule. Filed **EX-575**.
- [x] dismissed · `simplify` · `src/lib/kosztorys/seed-*.ts` · Altitude confirms the no-etap rule is at the right level and genuinely not duplicated — you cannot factor out "neither of these does the thing", and `0f3b338f` correctly deleted the conditional insert rather than hoisting it. The regression test now guards a collision impossible by construction; keep it, it pins the invariant. The duplicated _rationale_ was already dropped in Step 1.
- [x] dismissed · `simplify` · `src/lib/kosztorys/types.ts:91` · The `ToolPlaneT` merge itself: verified correct depth, **do not re-split**. `settlement.ts:167` and `calc.ts:52` feed the same value through as a `PriceViewT` — the three DB columns were named apart by history, not meaning. AGENTS.md's "one concept, one name" mandates it.
- [x] dismissed · `simplify` · diff-wide (reuse) · The extraction is **fully swept**: one `bg-transparent` in the tree (the primitive itself), zero read-only cell spans outside `ReadOnlyCellText`, one `key === 'Escape'` (the helper), zero `justify-start` outside `button.tsx`, zero rename state machines outside the hook. Five candidate dupes checked and rejected with reasons (`DecimalInput`, the dialog Enter handlers, `VAT_PLANE_LABELS`, `usePersistedEnum`, the `inputMode` props).

## Tests & suite

- `pnpm typecheck` — pass (twice: after the min-width fix, after the ToolPlaneT sweep).
- `pnpm exec vitest run src/__tests__/lib/kosztorys/` — 268 passed, 12 skipped (23 files).
  The 12 skips are DB-backed specs needing the `db-test` container on 5435, which is **not running**
  (`docker ps` → none). Two of them pin the new "seed installs no etap" contract, so that contract is
  currently unverified locally. Start it with `docker compose up -d db-test` + `pnpm db:import:test`.
- No new tests authored: both Step-1 correctness findings are CSS/rendered-class geometry inside a
  third-party grid (dispositions recorded on their lines — a jsdom class assertion would pin the
  implementation without proving the visual result). They owe a **browser check**, not a unit spec.
- `pnpm lint` — 0 errors (87 pre-existing warnings, all `db` unused-arg in `src/migrations/`).
- Full suite (`test:e2e` / `build`) **not run** — pending your go.

## Manual verification owed (blocks archive)

1. Sorted column header renders at weight 600, not 500.
2. Computed money cells vertically centre and line up with the editable cells in the same row.
3. The trailing gap column still renders ~48px wide, and every column still opens at ≥140px.
4. Header text is still xs/medium after the `globals.css` rule was deleted (the primitives moved in the same commit).
